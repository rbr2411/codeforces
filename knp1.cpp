#include <bits/stdc++.h>
using namespace std;
int main()
{
    int weight; 
    cin >> weight;
    int num_items;
    cin >> num_items;
    vector<int> v(num_items);
    for(int i=0;i<num_items;i++)
    {
        cin >> v[i];
    }
    
}